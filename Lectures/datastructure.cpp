/*

Stacks
    last in first out
    push and pop
    peek
    insert at front
    remove at fornt
Queues 
    First in first out
    insert - enqueue - add elements to the back
    delete - dequeue - remove elements from the front
    insert back
    remove back
    
Double ended queues
    double linked list
    insert back
    remove front

deque - double linked list 
    insert left - insert first
    insert right - insert last
    delete left - remove first
    delete right - remove last
    

queue - single linked list
    enqueue - insert last/back
    dequeue remove first/front
        

Arrays
    stack, last position is pop position
    cursor, at last postion
    queue - circular arrays
        search it up if u forget  
        
        
adapter
    new class using an old class
    

priority queue
    highest priority item is removed first


dymanic array
    if (n == capacity){
        capacity *= 2;
        string *temp = new string{capacity};
        
        for (int i = 0; i < n; i++){
            temp[i] = arr[i];
        }
        delete[] arr;
        arr = temp
    }
*/


